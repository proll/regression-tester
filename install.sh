bower install backstopjs;
cd bower_components/backstopjs;
npm install;
sudo npm install gulp -g;
sudo npm install phantomjs -g;
sudo npm install -g casperjs;
# if you want to reset config
# gulp genConfig
gulp reference;
gulp test;